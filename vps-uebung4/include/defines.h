#ifndef DEFINES_H
#define DEFINES_H

enum life_status_t {dead, alive, invalid};

const unsigned clientCount=7;
const unsigned boardHeight = 100;
const unsigned boardWidth = 100;
const unsigned steps=100;

#endif

